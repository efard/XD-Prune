Need to save:
1. For each models (L1, LR: 10%, 56%): unpruned baselines result, evaluation image count/list, source checkpoint.
2. The raw pruned pt, recovered pt, training args.yaml, pruning script, channel-selection log (methodology).
3. The mAP validation images/seed used (are images selected the same for every model tested or not).
4. The accuracy evaluations of NCNN models on server: script and confidence intervals used for the mAP calculation.


Scripts explainions:
# server
1. baseline-light training
2. profiling & error injection
3. baseline-full to global-L1 p10, p56
4. baseline-full to layer-replacement p10, p56
5. mAP50-95 measurment

# pynq-z2
1. ncnn size, speed measuring
2. ncnn measure runner
3. ncnn video runner from pc

# local-pc
1. pt to ncnn (defaults and int8)
2. ncnn video to pynq