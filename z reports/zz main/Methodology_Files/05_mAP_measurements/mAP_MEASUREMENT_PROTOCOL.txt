mAP MEASUREMENT SETTINGS

Main PT/NCNN metrics:
- mAP50-95
- mAP50
- mAP75
- Precision
- Recall

Ultralytics extraction:
  metrics.box.map   -> mAP50-95
  metrics.box.map50 -> mAP50
  metrics.box.map75 -> mAP75

Typical final validation settings used in the Global L1 recovery work:
  split = val
  imgsz = 640 unless evaluating a lower-resolution export
  batch = 16 for PT final validation
  FP32
  rect = True
  conf = 0.001
  iou = 0.70
  max_det = 300
  augment = False
  seed = 42

NCNN screening:
  batch = 1
  device = CPU/NCNN
  conf = 0.001
  iou = 0.70
  max_det = 300
  rect = True
  augment = False
  input size read from metadata.yaml

GEN NCNN screening used the SAME fixed 550-image subset for each GEN model: seed = 42
SNOW NCNN screening used the full fixed 100-image validation split.
