NCNN ACCURACY EVALUATION PROTOCOL

GEN validation: fixed 550-image subset, seed 42, same images for all GEN NCNN models.
SNOW validation: full 100-image fixed validation set.

Ultralytics validation settings:
split=val
batch=1
device=cpu
conf=0.001
iou=0.70
max_det=300
rect=True
augment=False
input size from each NCNN metadata.yaml

Outputs:
mAP50-95 = metrics.box.map
mAP50 = metrics.box.map50
mAP75 = metrics.box.map75
Precision, Recall, per-class AP50-95

mAP50-95 averages AP across IoU thresholds 0.50,0.55,...,0.95.
